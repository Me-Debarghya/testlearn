{
    "version": 2,
    "builds": [
      
        {
            "src": "server.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "dist/**"
                ]
            },
            "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ./database/"

        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.js"
        }
    ]
}